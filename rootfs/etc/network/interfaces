# interface file auto-generated by buildroot

auto lo
iface lo inet loopback

auto eth0
iface eth0 inet dhcp

auto eth1
iface eth1 inet dhcp

auto can0
iface can0 inet manual
up ifconfig can0 up
down ifconfig can0 down
pre-up ip link set can0 type can bitrate 500000 triple-sampling on

auto can1
iface can1 inet manual
up ifconfig can1 up
down ifconfig can1 down
pre-up ip link set can1 type can bitrate 500000 triple-sampling on

auto wlan0
iface wlan0 inet dhcp
pre-up wpa_supplicant -B -Dnl80211 -iwlan0 -c/etc/wpa_supplicant.conf
post-down killall -q wpa_supplicant

auto ppp0
iface ppp0 inet manual
pre-up pppd call quectel-ppp > /dev/null &
